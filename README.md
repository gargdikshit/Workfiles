Vagrant-3-tier-architecture
An all in one playbook to deploy a simple app and database using vagrant


Requirements
Vagrant 2.x.x


To run playbook
$ git clone https://github.com/gargdikshit/Workfiles.git
$ cd Wokfiles/Vagrant/#-tier
$ vagrant up 

OS Used:Ubuntu
